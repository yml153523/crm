# 功能变更记录: [功能名称] - CR-{序号}

## 0. 变更概览 (Change Overview)
*   **变更标题**: [简要描述变更内容]
*   **变更类型**: 微调 (Tweak) / 扩展 (Extension) / 重构 (Refactor)
*   **变更原因**: [为什么要做这个变更]
*   **发起日期**: [YYYY-MM-DD]
*   **开发方法**: TDD（测试驱动开发）— 每个任务按 Red-Green-Refactor 循环执行
*   **关联功能**: [原功能名称]
*   **关联文档**:
    -   需求文档: `specs/features/{功能名称}.md`
    -   技术方案: `specs/features/{功能名称}_技术方案.md`
    -   任务规划: `specs/features/{功能名称}_任务规划.md`

## 1. 影响分析 (Impact Analysis)

### 1.1 需求影响
| 影响项 | 变更类型 | 详情 |
| :--- | :--- | :--- |
| AC-{新编号} | 新增 | [新验收标准描述] |
| AC-{已有编号} | 修改 | [原内容] → [修改后内容] |

### 1.2 技术影响
| 影响层 | 影响范围 | 详情 |
| :--- | :--- | :--- |
| 数据层 | [新增字段/新增表/无影响] | [具体描述] |
| API 层 | [新增接口/修改接口/无影响] | [具体描述] |
| 表现层 | [新增组件/修改组件/无影响] | [具体描述] |
| 业务逻辑 | [新增逻辑/修改逻辑/无影响] | [具体描述] |

### 1.3 代码影响
| 文件路径 | 操作 | 影响说明 |
| :--- | :--- | :--- |
| `path/to/file.js` | 修改 | [需要修改的内容] |
| `path/to/new-file.js` | 新增 | [新文件用途] |

### 1.4 测试影响
| 测试文件 | 影响类型 | 说明 |
| :--- | :--- | :--- |
| `tests/unit/xxx.test.js` | 需修改 | [现有用例需调整的原因] |
| `tests/unit/yyy.test.js` | 无影响 | 不涉及变更范围 |
| `tests/unit/zzz.test.js` | 需新增 | [新增测试覆盖范围] |

### 1.5 回归风险评估
*   **高风险区域**: [列出可能因变更而出现回归问题的模块]
*   **已有测试覆盖**: [列出已有测试是否覆盖受影响区域]
*   **需要补充的测试**: [列出需要新增的测试覆盖范围]

## 2. 需求变更详情 (Requirements Delta)
> 仅记录本次变更涉及的需求变化，已有需求不重复列出

### 2.1 新增/修改的用户故事
- **US-{编号}**: 作为 [角色]，我希望 [行为]，以便 [价值]
    - 关联验收标准：AC-XXX, AC-XXX

### 2.2 新增/修改的验收标准

#### 正常流程 (Happy Path)
- **AC-{编号}**: [场景名称]
    - Given: [前置条件]
    - When: [用户操作]
    - Then: [预期结果]

#### 边界与异常 (Edge & Error Cases)
- **AC-{编号}**: [场景名称]
    - Given: [前置条件/异常状态]
    - When: [用户操作]
    - Then: [系统响应/错误提示]

#### 业务规则 (Business Rules)
- **AC-{编号}**: [场景名称]
    - Given: [前置条件]
    - When: [触发业务规则的操作]
    - Then: [规则执行的预期结果]

### 2.3 移除的内容（如有）
- [列出不再需要的功能点，标注原因]

## 3. 技术变更详情 (Technical Delta)
> 仅记录本次变更涉及的技术变化

### 3.1 数据库变更
```sql
-- 示例：新增字段
ALTER TABLE xxx ADD COLUMN yyy VARCHAR(255);
-- 或：创建新表
CREATE TABLE zzz (...);
```

### 3.2 API 变更
| 操作 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 新增 | POST | `/api/xxx` | [描述] |
| 修改 | GET | `/api/yyy` | [原行为] → [新行为] |

### 3.3 组件变更
| 操作 | 组件 | 说明 |
| :--- | :--- | :--- |
| 修改 | `components/xxx.jsx` | [变更描述] |
| 新增 | `components/yyy.jsx` | [新组件用途] |

### 3.4 兼容性说明
*   **向前兼容**: [是否兼容旧版本数据/接口]
*   **迁移方案**: [如果不兼容，迁移步骤是什么]

## 4. 增量开发任务 (Incremental Tasks)
> 任务编号从原任务规划最后一个编号之后继续
> 每个任务耗时 < 2h (120m)
> 每个任务按 TDD 循环执行：RED（写测试）→ GREEN（写实现）→ REFACTOR（重构）

### 阶段一：数据层变更 (Data Layer Delta)
> 如无数据层变更，跳过此阶段

- [ ] **Task-{N+1}**: [任务标题]
    *   **说明**: [具体描述]
    *   **变更类型**: 新增 / 修改
    *   **涉及文件**: `path/to/file`
    *   **测试文件**: `tests/unit/xxx.test.js`
    *   **参考**: 本文档 Sec 3.1
    *   **对应AC**: AC-{编号}
    *   **预估工时**: [X]m
    *   **依赖**: [已有任务编号或"无"]
    *   **验证标准**（TDD RED 阶段的测试依据）:
        - [ ] [具体的输入条件和预期输出]
        - [ ] [具体的边界/异常场景和预期行为]

### 阶段二：表现层变更 (Presentation Layer Delta)
> 如无表现层变更，跳过此阶段

- [ ] **Task-{N+2}**: [任务标题]
    *   **说明**: [具体描述]
    *   **变更类型**: 新增 / 修改
    *   **涉及文件**: `path/to/file`
    *   **测试文件**: `tests/components/xxx.test.jsx`
    *   **参考**: 本文档 Sec 3.3
    *   **对应AC**: AC-{编号}
    *   **预估工时**: [X]m
    *   **依赖**: [任务编号]
    *   **验证标准**（TDD RED 阶段的测试依据）:
        - [ ] [具体的输入条件和预期输出]
        - [ ] [具体的边界/异常场景和预期行为]

### 阶段三：业务逻辑变更 (Business Logic Delta)
> 如无业务逻辑变更，跳过此阶段

- [ ] **Task-{N+3}**: [任务标题]
    *   **说明**: [具体描述]
    *   **变更类型**: 新增 / 修改
    *   **涉及文件**: `path/to/file`
    *   **测试文件**: `tests/unit/xxx.test.js`
    *   **参考**: 本文档 Sec 3.2
    *   **对应AC**: AC-{编号}
    *   **预估工时**: [X]m
    *   **依赖**: [任务编号]
    *   **验证标准**（TDD RED 阶段的测试依据）:
        - [ ] [具体的输入条件和预期输出]
        - [ ] [具体的边界/异常场景和预期行为]

### 阶段四：回归验证 (Regression Verification)
> 每个增量变更必须包含回归验证

- [ ] **Task-{N+Last}**: 回归验证
    *   **说明**: 运行全量已有测试套件，确保变更未破坏原有功能。注意：每个增量任务内部已通过 TDD 循环完成了自身的测试编写，此处重点是验证跨模块的回归安全性。
    *   **变更类型**: 验证
    *   **涉及文件**: `tests/` 目录下所有相关测试文件
    *   **对应AC**: 所有受影响的 AC
    *   **预估工时**: 60m
    *   **依赖**: 上述所有增量任务
    *   **验证标准**:
        - [ ] 原有全量测试通过（无回归）
        - [ ] 本次变更的所有新增测试通过
        - [ ] 测试覆盖率未下降
        - [ ] 修改的已有测试仍然通过

## 5. 增量验收标准检查清单 (Incremental AC Checklist)
> 仅包含本次变更涉及的验收标准

| 验收标准ID | 验收标准描述 | 状态 | 对应任务 | 操作 |
| :--- | :--- | :--- | :--- | :--- |
| AC-{编号} | [描述] | 待完成 | Task-{N+x} | 新增 |
| AC-{已有编号} | [描述] | 待更新 | Task-{N+y} | 修改 |

## 6. 变更总结 (Change Summary)
*   **总新增任务数**: [N] 个
*   **预计总工时**: [X] 分钟（约 [Y] 小时）
*   **风险等级**: 低 / 中 / 高
*   **风险说明**: [如有风险，描述风险及缓解措施]
*   **测试影响**: [需修改 X 个已有测试，新增 Y 个测试]
*   **预期效果**: [变更完成后的预期效果]
